package com.example.shoppingcart;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic Spring Boot application context loading test.
 * Ensures that the application context loads successfully.
 */
@SpringBootTest
class ShoppingCartApplicationTests {

    @Test
    void contextLoads() {
        // This test simply checks if the Spring application context loads without errors.
        // It's a good sanity check to ensure all beans are wired correctly.
    }
}